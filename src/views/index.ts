import { renderHome } from './home';
import { renderMore } from './more';
import { renderInstall } from './install';
import { renderConfigure } from './configure';
import { renderRoot } from './root';

export {
  renderHome,
  renderMore,
  renderInstall,
  renderConfigure,
  renderRoot
};
